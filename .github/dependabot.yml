# Dependabot config for contextmatrix-backendkit.
# Canonical copy: contextmatrix-agent/.github/dependabot.yml - sync by
# full-file copy plus repo-name swap (minus the docker block; no Dockerfile
# in this repo).
# Tracks two ecosystems weekly:
#   - gomod          : Go module dependencies at the repo root.
#   - github-actions : pinned action versions in .github/workflows/*.
#
# Paired with CI (.github/workflows/ci.yaml). Dependabot bump PRs must pass the
# same gates as any other PR. Minor/patch bumps are grouped to cut PR noise.

version: 2

updates:
  - package-ecosystem: gomod
    directory: "/"
    schedule:
      interval: weekly
    open-pull-requests-limit: 10
    groups:
      minor-and-patch:
        update-types:
          - minor
          - patch
    labels:
      - dependencies
      - go

  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    open-pull-requests-limit: 10
    groups:
      minor-and-patch:
        update-types:
          - minor
          - patch
    labels:
      - dependencies
      - github-actions
